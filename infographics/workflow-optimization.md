# Infographic: Workflow Optimization

**Tagline:** Your pipeline is probably slower than it needs to be.

---

## Infographic A — Tangled vs. Clean
**Format:** Hard vertical split, landscape 1920×1080

**Left half — the broken pipeline:**
Visual metaphor: a dense tangle of overlapping arrows, branching paths, loops that go nowhere, dead ends — like a spaghetti diagram of a dysfunctional process. Rendered in cold grey-white lines on near-black. Feels chaotic, exhausting to look at.
Small label top-left: `YOUR PIPELINE NOW` in `#8a8a8a`

**Right half — after the audit:**
The same journey as a clean horizontal flow — five clear steps in a straight line, no tangles. Breathing room. Precise.
Small label top-right: `AFTER THE AUDIT` in `#da1313`

**Red dividing line** at center.

**Bottom:** `YOUR PIPELINE IS PROBABLY SLOWER THAN IT NEEDS TO BE.` white 700

---

## Infographic B — The Invisible Cost
**Format:** Four-quadrant dark grid, landscape

**Background:** Dark canvas. Thin `#333333` lines forming the 2×2 grid.

Each quadrant has one large background element at ~15% opacity suggesting its problem type — a broken link, duplicate files, a clock, mismatched connectors.

**Quadrant labels (center of each, minimal):**
- Top-left: `HANDOFF PROBLEMS` red · `20 minutes, every time` `#8a8a8a`
- Top-right: `REPEAT MANUAL WORK` red · `Hours per week, unmeasured` `#8a8a8a`
- Bottom-left: `VERSION CONFUSION` red · `Decisions on outdated material` `#8a8a8a`
- Bottom-right: `TOOL FRICTION` red · `Software that won't talk to each other` `#8a8a8a`

**Center intersection:** small red circle with `×4` — these four multiply each other.

**Bottom:** `WE FIND THEM. QUANTIFY THEM. FIX THEM IN ORDER OF IMPACT.`

---

## Infographic C — The Audit
**Format:** Minimal process flow over atmospheric background

**Background:** Faint photograph of a studio workspace — monitors, files, a desk — at 10% opacity.

**Five steps** as large horizontal labels with red step numbers:
`01 DISCOVERY CALL` · `02 AUDIT` · `03 WRITTEN REPORT` · `04 QUICK WINS` · `05 BUILD & TRAIN`

Step `04 QUICK WINS` is highlighted — brighter, slightly larger, red underline.

**Below step 04:** `Start saving time this week.` `#da1313`

**Below the flow:** `from €1,500 · Pipeline audit + written report · The report is yours.`
`#8a8a8a` small
