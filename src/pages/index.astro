---
// Bare-root redirect → default locale.
// All real content lives under /en/ and /hu/. This file exists so a visitor
// landing on visiongraphics.eu without a locale prefix is sent to the EN
// version (per our decision: no automatic browser-locale detection).
return Astro.redirect('/en/', 308);
---
