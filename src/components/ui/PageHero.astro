---
// src/components/ui/PageHero.astro
//
// Two variants — matched to dev.visiongraphics.eu measured heights:
//
//   variant="page"    → ~560px  — top-level section pages (services, portfolio, about…)
//   variant="section" → ~420px  — service sub-pages and detail pages
//
// Props:
//   image    — path to banner image, e.g. "/_img/banners/banner-services.jpg"
//   label    — small red line 1, e.g. "Visuals & Solutions"
//   title    — large bold white line 2, e.g. "Show It, Scale It, Build It."
//   variant  — "page" (default) | "section"

interface Props {
  image:    string;
  label:    string;
  title:    string;
  variant?: 'page' | 'section';
  imageAlt?: string;
}

const { image, label, title, variant = 'page', imageAlt = '' } = Astro.props;
---

<div class={`page-hero page-hero--${variant}`}>
  <div class="page-hero__bg">
    <img src={image} alt={imageAlt} loading="eager" decoding="async" />
  </div>
  <div class="page-hero__content container">
    <p class="page-hero__label">{label}</p>
    <h1 class="page-hero__title">{title}</h1>
  </div>
</div>

<style>
  /* ── Shared base ─────────────────────────────────────────── */
  .page-hero {
    position: relative;
    width: 100%;
    overflow: hidden;
    background: var(--color-surface);
    border-bottom: 1px solid var(--color-border);
  }

  /* ── Variant heights (½ page, ¾ section of original dev heights) ──── */
  .page-hero--page    { height: clamp(160px, 21vw, 280px); }
  .page-hero--section { height: clamp(195px, 25vw, 315px); }

  /* ── Background image ───────────────────────────────────── */
  .page-hero__bg {
    position: absolute;
    inset: 0;
  }

  .page-hero__bg img {
    position: absolute;
    inset: 0;
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center right;
    display: block;
  }

  /* Dark gradient: solid on left, transparent on right */
  .page-hero__bg::after {
    content: '';
    position: absolute;
    inset: 0;
    background: linear-gradient(
      to right,
      var(--color-bg) 0%,
      var(--color-bg) 28%,
      rgba(26, 26, 26, 0.80) 52%,
      rgba(26, 26, 26, 0.28) 72%,
      transparent 100%
    );
  }

  /* ── Text content ───────────────────────────────────────── */
  .page-hero__content {
    position: relative;
    z-index: 1;
    height: 100%;
    display: flex;
    flex-direction: column;
    justify-content: center;
    gap: var(--space-2);
    padding-block: var(--space-10);
    max-width: 60%;
  }

  .page-hero__label {
    font-family: var(--font-body);
    font-weight: 700;
    color: var(--color-accent);
    letter-spacing: 0.02em;
    margin: 0;
    line-height: 1.2;
  }

  .page-hero__title {
    font-family: var(--font-body);
    font-weight: 700;
    color: var(--color-text);
    line-height: 1.02;
    letter-spacing: -0.025em;
    margin: 0;
  }

  /* ── Variant font sizes ─────────────────────────────────── */
  .page-hero--page .page-hero__label   { font-size: var(--fs-small); }
  .page-hero--page .page-hero__title   { font-size: var(--fs-h2); }

  .page-hero--section .page-hero__label { font-size: var(--fs-xs); }
  .page-hero--section .page-hero__title { font-size: var(--fs-h4); }

  /* ── Mobile ─────────────────────────────────────────────── */
  @media (max-width: 640px) {
    .page-hero--page    { height: clamp(130px, 27.5vw, 170px); }
    .page-hero--section { height: clamp(150px, 34vw, 210px); }

    .page-hero__content {
      max-width: 90%;
      padding-inline: 0;
    }

    .page-hero__bg::after {
      background: linear-gradient(
        to right,
        var(--color-bg) 0%,
        var(--color-bg) 45%,
        rgba(26, 26, 26, 0.88) 70%,
        rgba(26, 26, 26, 0.65) 100%
      );
    }
  }
</style>
